/** Bundled portable skill — rendered from agent-content.ts (keep skills/plugdev in sync). */

import { renderSkillAgentsMd, renderSkillMd } from "./agent-content.js";

export const SKILL_MD = renderSkillMd();
export const SKILL_AGENTS_MD = renderSkillAgentsMd();
